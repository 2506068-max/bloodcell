{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "vite: dev server",
      "type": "shell",
      "command": "try { (Invoke-WebRequest -Uri 'http://localhost:5173/' -UseBasicParsing -TimeoutSec 2) | Out-Null; Write-Host '  Local:   http://localhost:5173/'; while ($true) { Start-Sleep -Seconds 3600 } } catch { npm run dev -- --host 0.0.0.0 --strictPort }",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "isBackground": true,
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "clear": false
      },
      "problemMatcher": {
        "owner": "custom",
        "pattern": {
          "regexp": ".+"
        },
        "background": {
          "activeOnStart": true,
          "beginsPattern": ".*",
          "endsPattern": ".*Local:\\s+http://localhost:5173/.*"
        }
      }
    }
  ]
}
